import { Lowercase } from './lowercase';

describe('TransformeLowercase', () => {
  it('should create an instance', () => {
    const directive = new Lowercase();
    expect(directive).toBeTruthy();
  });
});
